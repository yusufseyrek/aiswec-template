# A project built with AISWEC

Open it in Companion, ask for what you want in plain words, approve the plan AISWEC writes,
and accept the changes when they are ready. Every irreversible step is your click.

`bun test` runs the checks AISWEC passes before changes reach you; `.github/aiswec.yml` is
how it behaves here, edited from Companion's Settings page.
